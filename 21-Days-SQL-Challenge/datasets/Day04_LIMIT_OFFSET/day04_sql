-- Day 4: LIMIT and OFFSET

-- 1. Display first 5 patients
SELECT * FROM patients LIMIT 5;

-- 2. Show patients 11–20 using OFFSET
SELECT * FROM patients LIMIT 10 OFFSET 10;

-- 3. Get the 10 most recent patient admissions
SELECT * FROM patients
ORDER BY arrival_date DESC
LIMIT 10;

-- Day 4 Challenge:
-- Find the 3rd to 7th highest satisfaction scores
SELECT patient_id, name, service, satisfaction
FROM patients
ORDER BY satisfaction DESC
LIMIT 5 OFFSET 2;
